# WrenAI（新框架 0.13.4）五大入口分步操作指南

> 对象：`pip install wrenai` 的 **agent 驱动新框架**（wrenai 0.13.4，本机实测 CLI help + `wren skills get` 全文）。
> **不含**老框架（docker-compose 多容器 chat BI，已冻结在 legacy/v1，本文不涉及）。
> 覆盖五个主题：`wren context *`、`wren profile *`、`wren context import dbt`、`wren serve mcp`、Skill: onboarding。
> 每一步写清：**输入什么 → 跑什么命令 → 输出/写了哪些文件 → 底层调的是哪个模块**。
> 源码位置：`C:\Python314\Lib\site-packages\wren\`（PyPI `wrenai` = 上游仓 `core/wren/`，引擎为 `core/wren-core` Rust，经 wren-core-py 进程内调用，**无独立引擎服务进程**）。

## 全局心智模型（先记这张图）

```
~/.wren/  全局层                     <project>/  项目层（YAML 源）
├── profiles.yml  连接配置(占位符)     ├── wren_project.yml   项目元数据 + profile pin
├── config.yml    default_project     ├── models/*/metadata.yml
└── .env          秘密(可选)          ├── views/*/  cubes/*/  relationships.yml
                                    ├── knowledge/  (rules/*.md, sql/*.md)
         wren profile * 管左边        └── AGENTS.md
                                    wren context * 管右边
                                              │ wren context build
                                              ▼
                                    <project>/target/mdl.json  (camelCase 编译产物)
                                              │ 加载
                                              ▼
                                    WrenEngine (Rust wren-core, 进程内)
                                     ↙ wren --sql    ↖ wren serve mcp (stdio/http)
```

**分工**：`profile` 管"连哪个库"（全局一份），`context` 管"数据是什么意思"（每项目一份 YAML），`build` 把 YAML 编译成引擎唯一认的 `target/mdl.json`，`serve mcp` 把这套能力以 17 个 MCP tools 暴露给 Claude/Cursor，`onboarding` skill 是把前四者串成一条流的 agent 工作流指南。

---

## 一、wren context * —— 项目（MDL 语义层）生命周期

子命令：`init / validate / build / show / set-profile / instructions / upgrade / import`。

### 1.1 wren context init —— 脚手架

```bash
wren context init [--path <dir>] [--empty] [--from-mdl <file>] [--from-osi <file> --data-source <ds>] [--force]
```

**输入**：目标目录（缺省 = cwd；发现顺序 `--path` > `WREN_PROJECT_HOME` env > 从 cwd 向上找 `wren_project.yml` > `~/.wren/config.yml` 的 default_project）。

**输出**（写入项目目录，`context_cli.py: init()`）：

```
<project>/
├── wren_project.yml      # schema_version: 5 / name / catalog: wren / schema: public / data_source
├── relationships.yml     # 空 join 定义（带注释示例）
├── models/example/metadata.yml       # 示例模型（--empty 跳过，给 agent 自己填时用）
├── views/example_view/{metadata.yml, sql.yml}
├── cubes/
├── knowledge/{knowledge.yml, rules/general.md, glossary/ metrics/ caveats/ sql/}
└── AGENTS.md             # 给 AI agent 的工作流指引
```

**易混点**：`catalog/schema` 是 Wren Engine 自己的命名空间，不是数据库的；库的真实位置在每个 model 的 `table_reference` 里。
**三种来源**：空白脚手架 / `--from-mdl`（camelCase MDL JSON 反向拆成 YAML，`convert_mdl_to_project()`）/ `--from-osi`（Open Semantic Interchange YAML 单向转换，需 `--data-source`）。
**拒绝覆盖**：已有 `wren_project.yml` 时直接报错，要 `--force`。

### 1.2 wren context validate —— 只读校验

**输入**：项目 YAML（不写任何东西）。**输出**：结构错误清单。校验点（`validate_project()`）：模型必须有 name/columns、`table_reference` 与 `ref_sql` 二选一、relationship 引用的模型必须存在、cube 的 base_object 必须是已定义 model/view、视图 SQL 会做 dry-plan。

### 1.3 wren context build —— 编译

**输入**：`wren_project.yml` + `models/` + `views/` + `cubes/` + `relationships.yml`（loader 分别为 load_models/load_views/load_cubes/load_relationships；`ref_sql.sql`、`sql.yml` 独立文件优先于 metadata.yml 内联）。**输出**：`<project>/target/mdl.json` —— 全部合并成单一 manifest，key 经 `_convert_keys()` snake_case→camelCase，盖 `layoutVersion: 3`。**注意 `knowledge/` 不进 mdl.json**（rules 运行时单独读）。默认先 validate 再编译。

### 1.4 wren context show / instructions / set-profile / upgrade

| 命令 | 输入 | 输出 |
|------|------|------|
| `show` | 项目 YAML | 终端列出 models/views/relationships（agent 数模型用：`wren context show \| grep -c '^model:'`） |
| `instructions` | `knowledge/rules/` + legacy instructions.md | 打印业务规则给 LLM 消费（"规则覆盖默认行为"） |
| `set-profile <名>` | `~/.wren/profiles.yml` | 往 `wren_project.yml` 写 `profile:` + `data_source:` 两行（见 §2.4） |
| `upgrade` | 旧 schema_version 项目 | v1→v2（平文件→目录式）、v4→v5（补 knowledge 骨架）等迁移 |

---

## 二、wren profile * —— 连接配置（全局，与项目解耦）

子命令：`list / add / import / rm / switch / debug`。唯一存储：`~/.wren/profiles.yml`（`WREN_HOME` 可改）。

### 2.1 wren profile add —— 四种姿势

```bash
wren profile add <名字> --from-file /tmp/conn.yml   # agent 主用（onboarding skill Step 3）
wren profile add <名字> --interactive               # 终端逐字段问答（字段清单来自共享 field_registry）
wren profile add --ui                               # 起本地 starlette/uvicorn 服务自动开浏览器表单
wren profile add <名字> --datasource postgres       # inline 最小 profile，提示手编 profiles.yml
```

**--from-file 的输入格式**：只接受 flat（`{datasource, host, port, ...}`）或 `{datasource, properties: {...}}` 信封两种 shape，其它嵌套拒绝。**字段名不许臆造**——先用 `wren docs connection-info <ds> --format md` 看真字段（从活 Pydantic schema 内省的）。
**输出**：写 `~/.wren/profiles.yml`（临时文件+原子替换，权限 0600），结构：

```yaml
active: <名>
profiles:
  <名>:
    datasource: postgres
    host: ${PG_HOST}      # ${VAR} 占位，真值永不落盘
    password: ${PG_PW}
```

**保存后自动连库验证**（`SELECT 1`；`--no-validate` 跳过）。注意：**同名静默覆盖，没有 --force**。

### 2.2 秘密解析（安全设计核心）

- `${VAR}` 只匹配 UPPER_SNAKE_CASE（`_SecretTemplate` 自定义正则），小写 `${foo}` 当字面量。
- 占位符**永不落盘替换**：`expand_profile_secrets()` 只在连接瞬间解析，来源优先级 = 已 export 的 env > `$CWD/.env` > 项目根 `.env` > `~/.wren/.env`；解析不到抛 `MissingSecretError`（fail-loud）。
- `wren profile debug`：输出前对 SecretStr 字段+名称启发式双重打码。

### 2.3 wren profile list / switch / rm / debug

| 命令 | 作用 |
|------|------|
| `list` | 列出全部 profile，高亮 active |
| `switch <名>` | 只改全局 active |
| `rm <名>` | 删除 |
| `debug` | 打码后的解析结果（排连接问题第一步） |

### 2.4 profile ↔ 项目的绑定与解析顺序

`wren context set-profile <名>` 把 `profile: <名>` + `data_source: <ds>` 写进项目的 `wren_project.yml`。运行时解析（`resolve_profile_for_project()`）：

```
wren_project.yml 的 profile: 字段（项目级 pin，优先）→ pin 了但 profile 不存在 = 报错，不静默回退
没有 pin → ~/.wren/profiles.yml 的 active（全局默认）
```

即 **项目 pin > 全局 active**；`profile switch` 不影响已 pin 的项目。onboarding skill 特意做这一步，就是保证以后多项目并存时互不串线。

---

## 三、wren context import dbt —— dbt 项目一次性编译导入

```bash
wren context import dbt --project-dir <dbt项目> [--profiles-path ...] [--profile ...] [--target ...] [--dry-run] [--force]
```

**输入**（`load_dbt_artifacts()`，`dbt.py:261`）：

| 文件 | 必需 | 用途 |
|------|------|------|
| `<dbt>/dbt_project.yml` | ✅ | 项目名、profile 名、target-path |
| dbt 的 profiles.yml（`--profiles-path` / `DBT_PROFILES_DIR` / `~/.dbt/`） | ✅ | 解析 target → 数据源类型 |
| `<dbt>/target/manifest.json` | ✅ | 模型/源的结构与列（逻辑层） |
| `<dbt>/target/catalog.json` | ✅ | 数据库真实列（物理层；缺了提示先 `dbt docs generate`） |
| `target/run_results.json`、`target/compiled/**/*.sql` | 可选 | test 状态 / 编译 SQL |

**输出**：一个**标准 wren 项目**（models/、relationships.yml、knowledge/、wren_project.yml、AGENTS.md），`--dry-run` 只预览不写。映射规则（`convert_dbt_project_to_wren_project()`）：

- **模型**：manifest 的 model 节点（跳过 ephemeral）→ `models/<name>/metadata.yml`；**列以 catalog.json 为准**；类型按 dbt adapter 方言转 MDL 类型（doris→mysql、sqlserver→tsql 特判）。
- **源**：模型名加 `raw_` 前缀。
- **关系**：从 dbt **test** 推导——`relationships` 测试 → `relationships.yml`（join_type 按层次：fct_→dim_ 为 MANY_TO_ONE）；`not_null`/`unique` → 列约束+主键；`acceptedvalues` → 列 properties。
- **分层**：按 stg_/int_/fct_/dim_ 前缀标 `dbt_layer`。
- **知识**：生成 `knowledge/rules/general.md` + 每模型种子 NL→SQL 对 `knowledge/sql/<slug>.md`。
- **绑定**：`wren_project.yml` 记 `dbt: {project_dir, profile, target}`。

**配套**：`wren profile import dbt` 把 dbt target 的连接转成 wren profile（BigQuery keyfile 会 base64）。
**性质**：**一次性快照导入，非运行时依赖**——dbt 侧后续变化不自动同步。导入后生命周期与手写项目完全一样（validate → build → 查询/serve）。

---

## 四、wren serve mcp —— 把语义层暴露给 AI 客户端

```bash
wren serve mcp [--transport stdio|http] [--host 127.0.0.1] [--port 8080]
               [--project <路径>] [--profile <名>]
               [--allow-write] [--no-connect] [--quiet]
```

**输入**：① `target/mdl.json`（**硬性要求**，缺了报错让你先 `wren context build`；源文件比它新会打 stale 警告——mtime 比对 `wren_project.yml`/`relationships.yml`/models/views/cubes）；② profile（`--profile` > 项目 pin > 全局 active），`${VAR}` 此刻解析。

**启动流程**（`serve_cli.py: serve_mcp()`）：定位项目 → 检查 mdl.json → 解析连接 → `_build_engine()` 用 mdl.json + connection 构**进程内 WrenEngine**（Rust，不依赖外部引擎服务）→ FastMCP server 按 transport 跑（stdio 默认；http = streamable-http `127.0.0.1:8080/mcp`）。启动时向 stderr 打各客户端注册命令（`claude mcp add wren -- wren serve mcp --project <p>` 等），stdio 下 stdout 是协议通道绝不污染。

**输出 = MCP 服务面**（`mcp_server.py`）：

- **Tools 17 个**（按 flag 条件注册）：
  - 查询：`run_sql`（经语义层执行，默认 1000 行/硬上限 10000）、`dry_run`、`query_cube`（结构化度量查询，`sql_only` 只出 SQL）——`--no-connect` 时这三个不注册
  - context：`get_mdl`/`list_models`/`describe_model`/`get_data_source`/`list_cubes`/`describe_cube`/`list_functions`
  - knowledge：`get_instructions`/`describe_schema`/`get_context`/`recall_queries`/`list_stored_queries`/`list_knowledge`
  - 写：`store_query`（NL→SQL 对写 `knowledge/sql/*.md` + LanceDB 索引）——仅 `--allow-write` 时注册
  - `dry_plan`（MDL 展开成方言 SQL，不连库）恒有
- **Resources 5 个**：`wren://mdl`、`wren://instructions`、`wren://project`、`wren://agents`、`wren://knowledge/{name}`
- **Prompts 1 个**：`wren_workflow` —— SOP：list_models → get_instructions → recall_queries → dry_run → run_sql → store_query

**客户端视角调用链**：

```
claude mcp add wren -- wren serve mcp --project <p>   （注册一次）
→ 客户端按需 spawn 进程（stdio）或连 http://host:port/mcp
→ initialize / tools list
→ 用户提问 → 模型按 wren_workflow 依次调 tools → WrenEngine 把 MDL 名展开成方言 SQL 下发数据库
```

**容错细节**：`get_mdl`/`query_cube` 等是现场从 YAML 重 build 的，对"忘了 rebuild"有一定容错；但查询引擎用的仍是启动时加载的旧 manifest——改了 YAML 记得重启。

---

## 五、Skill: onboarding —— 串起前面四者的 agent 工作流

`wren skills get onboarding` 输出一份 YAML frontmatter + markdown 的 **agent 工作流指南**（172 行，内容随安装版本走，不会文档漂移）。6 个 skill 之间的关系：

| skill | 作用 |
|-------|------|
| **onboarding** | 从零端到端：环境检查→项目脚手架→连接→首查 |
| generate-mdl | 探库生成 MDL（onboarding Step 4 手交给它） |
| usage | 日常查数工作流（onboarding 结束后转它） |
| enrich-context | 用业务上下文充实项目（Know） |
| genbi | 生成浏览器端 dashboard 并部署（Deploy） |
| dlt-connector | 接 SaaS 数据源 |

### 运行纪律（skill 开头的硬规则）

- **一轮只走一步**：每个编号步骤单独一轮，只问该步需要的信息，绝不一次收集项目名+库类型+凭据。
- **绝不在聊天里要凭据**（host/port/password/token 都不行）——凭据只走 `.env`，用户在编辑器里填，agent 永远看不到值。
- **MDL 没建好绝不查库**。
- **绝不臆造连接字段名**——永远先 `wren docs connection-info <ds>` 看真字段。
- 详细过程文档（安装/连接/排错 playbook）都在 docs 里，skill 只管规则和分流，不复制内容。

### 分步流程（每步：输入→命令→输出）

**Preflight（环境检查，只读不问）**
输入：无。命令：`python3 --version`（要 ≥3.11）、venv 检查、`wren --version`、`pwd`。输出：4 条发现清单。

**分支**：先试内置 `jaffle_shop` demo（30 秒免数据库）还是连自己的库？demo → 转 quickstart.md 结束；own DB → 继续。

**Step 1 收集项目名 + 库类型**（就问这两个，别的不要）
输入：用户回答两个问题。命令：`wren docs connection-info`（无参 = 列全部数据源）。输出：确定 `<project>` 与 `<ds>`。

**Step 2 工作区 + .env（批量执行）**
命令：`mkdir -p ~/<project> && cd` → `pip install "wrenai[<ds>,main]"` → **`wren docs connection-info <ds> --format md`**（内省连接器真实字段）→ 据此生成**空值** `.env`（键 = `<DS>_<FIELD>` UPPER_SNAKE，如 `POSTGRES_HOST=`）→ `.gitignore` 加 `.env`、建议 `chmod 600`。
输出：目录 + 空模板 `.env`。然后**停下**，让用户填完回一句 "done"。

**Step 3 建 profile**（用户回复 done 后才动）
输入：写 `/tmp/conn.yml`，**每个字段都是 `${VAR}` 占位**、键与 `.env` 一一对应：

```yaml
datasource: postgres
host: ${POSTGRES_HOST}
port: ${POSTGRES_PORT}
```

命令：`wren profile add <project> --from-file /tmp/conn.yml`（自动连库验证，即 §2.1）。输出：`~/.wren/profiles.yml` 新 profile；有 warning 查 connect.md#troubleshooting。

**Step 3.5 脚手架**：`wren context init --empty`（§1.1；拒绝覆盖已有项目）。

**Step 3.6 绑定**：`wren context set-profile <project>`（§2.4；写 pin 两行，此后查询连接与全局 active 无关）。

**Step 4 生成 MDL（交接给 generate-mdl skill）**
`wren skills get generate-mdl` → agent 探库、类型归一、写 models/ YAML。回来后跑：

```bash
wren context validate    # 输出：错误/警告清单
wren context build       # 输出：target/mdl.json
```

汇报模型数；`wren context show | grep -c '^model:'` ≥200 时建议 `pip install "wrenai[memory]"` + `wren memory index`（LanceDB，~800MB）。

**Step 5 收尾（转 usage skill）**：基于发现的表建议 2-3 个自然语言问题；日常查数转 `wren skills get usage`。

### 日常查数循环（usage skill 的核心，供对照）

```
wren memory fetch -q "<问题>"            # 取 schema 上下文（无 memory 则读 target/mdl.json）
wren context instructions               # 首问时取业务规则
wren memory recall -q "<问题>" --limit 3 # 召回历史 NL→SQL 做 few-shot
wren dry-plan --sql '...'               # 复杂查询先看展开 SQL（不连库）
wren --sql '...'                        # 执行（SQL 写 MDL 模型名，方言中立）
wren memory store --nl "..." --sql "..." # 默认沉淀（失败/用户说错/探索性查询不存）
```

错误恢复三板斧：`table not found` → memory fetch 查真名；连接错 → `wren profile debug` + `wren --sql "SELECT 1"`；SQL 错 → 先 `dry-plan` 分层定位（dry-plan 挂 = MDL 层问题；dry-plan 过但执行挂 = 数据库/方言层问题）。

---

## 附：五主题读写速查

| 命令 | 读 | 写 |
|------|-----|-----|
| `wren context init` | （--from-mdl/--from-osi 时：外部文件） | wren_project.yml、relationships.yml、models/、views/、knowledge/ 骨架、AGENTS.md |
| `wren context validate/show/instructions` | 项目 YAML | —（只读） |
| `wren context build` | wren_project.yml、models/、views/、cubes/、relationships.yml | target/mdl.json |
| `wren context set-profile` | ~/.wren/profiles.yml | wren_project.yml（profile+data_source 两行） |
| `wren context import dbt` | dbt_project.yml、dbt profiles.yml、target/{manifest,catalog}.json、run_results.json（可选） | 整套 wren 项目 |
| `wren profile add/list/switch/rm/debug` | --from-file 的 JSON/YAML、.env | ~/.wren/profiles.yml（原子 0600） |
| `wren profile import dbt` | dbt_project.yml + dbt profiles.yml | ~/.wren/profiles.yml |
| `wren serve mcp` | target/mdl.json（必需）、wren_project.yml（pin）、profiles.yml、.env、knowledge/、YAML 源（get_mdl 现场重 build） | knowledge/sql/*.md（store_query，需 --allow-write） |

> 相关文档：同仓《wren-ai-链路与集成调研.md》（同五主题的源码级机制细节）、《wrenai-项目链路与集成调研-20260909.md》（上游项目全貌与演进史）。
