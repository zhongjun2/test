# WrenAI（Canner/WrenAI）项目链路与集成调研

> 调研对象：https://github.com/Canner/WrenAI （2026-09 快照，README + 仓库结构）
> 姊妹篇：《wren-ai-链路与集成调研.md》——那篇调研的是 **`wrenai` CLI 0.13.4 本机实测**（context/profile/mcp 三层机制）；本篇补齐 **上游项目本身**的全貌：项目定位、代码结构、产品形态、演进历史、集成生态。
> 两篇合起来 = 「上游项目是什么 + 装到本机后怎么运转」。

## 1. 项目定位

WrenAI 自称 **开源 GenBI（Generative BI）引擎**：让 AI agent 在受治理的语义层上做 text-to-SQL、生成并部署可分享的 dashboard。核心卖点：

- **不只是裸 LLM 写 SQL**：SQL 必须经过 MDL 语义层规划 + dry-plan 校验 + 结构化报错，"governed text-to-SQL"。
- **不只是语义层**：在 schema 之外补了一层 **AI context layer**（业务定义、枚举、单位、批准的 join、历史问题记忆、非结构化公司知识），以 Git 友好的文件形式存在，可 review 可版本化。
- **面向 agent 而非面向人**：入口是 CLI + MCP + skills stub，设计为被 Claude Code / Cursor 等 agent 驱动，而不是又一个给人点的 BI UI。

一句话：**WrenAI = MDL 语义层（管"数据是什么意思"）+ AI 上下文层（管"业务上什么是对的"）+ 受治理执行引擎（管"SQL 别跑歪"），三者开源（Apache-2.0），agent 来当司机。**

## 2. 历史演进（重要：形态大变过）

| 时间 | 事件 |
|------|------|
| 早期 | Docker compose 一把梭的 **chat-first BI 产品**（wren-ui + wren-ai-service + wren-engine + pgadmin 多容器） |
| 2026-05-07 | **wren-engine（Rust）并回本仓** `core/`，原 `Canner/wren-engine` 仓归档 |
| 同期 | 产品形态转向 **agent 驱动**：Docker 版 chat 产品冻结为 **Wren GenBI Classic**，放 `legacy/v1` 分支（tag `v1-final`），不再加新功能/安全修复 |
| 现在 | 主线 = `pip install wrenai` CLI + `npx skills add Canner/WrenAI` + `wren serve mcp`，GenBI UI/dashboards 转商业版 |

**调研/选型时要认清：网上老文章（包括很多中文教程）讲的 docker-compose 多容器架构已经是 legacy/v1 的东西，与当前主线不是一套。**

## 3. 仓库结构

```
Canner/WrenAI
├── core/
│   ├── wren-core/         Rust 语义引擎（基于 Apache DataFusion）
│   ├── wren-core-base/    共享 manifest 类型 + MDL builder
│   ├── wren-core-py/      Python 绑定（PyPI: wren-core）
│   ├── wren-core-wasm/    WebAssembly 构建（npm: wren-core-wasm）→ 浏览器端跑引擎
│   ├── wren/              Python SDK + CLI（PyPI: wrenai）★ 姊妹篇调研的对象
│   └── wren-mdl/          MDL JSON schema
├── sdk/
│   └── wren-langchain/    参考 agent SDK 集成（LangChain/LangGraph）
├── skills/                给 AI agent 的 context 编写技能
├── docs/                  模块文档
└── examples/              示例项目
```

要点：
- **引擎内核是 Rust**（wren-core，DataFusion 之上），Python CLI 通过 wren-core-py 进程内调用——没有独立的引擎服务进程（legacy 时代的 ibis-server 形态已弃）。
- **wren-core-wasm** 让引擎能跑在浏览器里：这是 "Deploy" 一节 agent 生成浏览器端 dashboard 的技术底座。
- PyPI 上的 `wrenai` 包 = `core/wren/`，即姊妹篇实测的那套 `wren context/profile/serve` CLI。

## 4. 产品三拍：Generate · Deploy · Know

README 把 GenBI 能力归纳为三步：

### 4.1 Generate（生成）
Agent 把业务问题转成受治理 SQL + 图表。链路：
**schema 感知检索 → MDL 规划 → dry-plan 校验 → 结构化报错（带修复提示）→ 受限执行（行数上限）**。
对应 CLI：`wren query --sql '...'`、`wren ask "<问题>" --guided|--direct`（guided 给弱 agent 包好提示词，direct 给强 agent）。

### 4.2 Deploy（部署）
把任意查询答案变成**浏览器端可交互 dashboard**（wren-core-wasm 驱动），一条命令发到用户自己的 Vercel / Cloudflare Pages，得到可分享 URL。对应技能：`wren skills get genbi`。

### 4.3 Know（知识沉淀）
让答案正确的知识以**可版本化、带证据链接的文件**存在：
- 语义模型（MDL：models/views/cubes/relationships）
- 业务定义 `instructions.md`、确认过的 NL→SQL 例句（queries / knowledge）
- 本地 **LanceDB 记忆索引**（混合检索），markdown 是真源、LanceDB 只是索引副本

对应技能：`wren skills get enrich-context`（grill 逐问模式 / auto-pilot 自动模式，产物全部可 git review）。

## 5. 典型使用链路（agent 驱动）

```
① pip install wrenai              # 装 CLI（DuckDB 内置；postgres/memory 等 extras 按需）
② npx skills add Canner/WrenAI    # 给 Claude Code/Cursor/Cline/Codex 装 ~50 行发现 stub
③ 对 agent 说"用 Wren 接好我的 Postgres"
   → agent 跑 wren skills get onboarding，按内置指南逐步：
     环境检查 → 建 profile（连接配置）→ 脚手架项目 → 跑通首条查询
④ （可选）"用 raw/ 里的业务上下文充实项目" → enrich-context 技能
⑤ 问数："这个季度销售额 top 10 客户是谁？"
   → agent：拉 MDL 上下文 → 召回相似历史查询 → 写受治理 SQL → wren query 执行
⑥ "把它做成可筛选分享的 dashboard 发到 Vercel" → genbi 技能 → 浏览器端 app + 分享 URL
```

设计哲学：**工作流指南内嵌在 CLI 里按需下发（`wren skills get`），stub 只有 50 行**——指南内容永远与安装版本一致，不会文档漂移。

## 6. 集成生态

| 维度 | 支持情况 |
|------|---------|
| 数据源 | **22+**（Apache DataFusion 引擎）：BigQuery、Snowflake、PostgreSQL、ClickHouse、Redshift、Databricks、DuckDB 等；连接器分三级（官方 / 社区祝福 / 社区维护） |
| LLM | 无绑死自家服务——`wren ask` 生成的是"包装好的提示词"，由你自己 agent 的模型执行；旧版 ai-service 时代的多 pipeline 架构已随 legacy/v1 冻结 |
| Agent 框架 | MCP server（`wren serve mcp`，stdio/http，17 tools + 5 resources + 1 prompt，详见姊妹篇 §3.2）；skills stub 自动识别 Claude Code/Cursor/Cline/Codex；SDK 参考 `sdk/wren-langchain`（LangChain/LangGraph）+ `wren-pydantic` |
| dbt | `wren context import dbt`：manifest/catalog → MDL 一次性编译导入（详见姊妹篇 §3.1，快照式、非运行时依赖） |
| 部署 | 纯本机 pip；dashboard 发 Vercel/Cloudflare Pages；中国大陆有清华 pip 镜像 + HF_ENDPOINT=hf-mirror 的官方提示 |

## 7. 开源边界（open core）

**OSS（Apache-2.0，本仓）**：MDL 语义层、受治理 text-to-SQL、MCP server、CLI、22+ 连接器、引擎内核、wasm。

**商业（Wren AI Cloud / 自托管 Enterprise Plus）**：
- 行级/列级安全（RLS/CLS）+ 用户组权限
- GenBI UI、dashboards、嵌入与 API
- 场景化 AI（GenBI Apps、Agentic Mode、AI 辅助上下文准备）
- 高级安全审计、SLA、云/VPC/物理隔离部署

选型注意：**想要"给人用的聊天 BI 界面"在 OSS 里已经没有了**（那是 legacy/v1 或商业版）；OSS 主线的交付物是"让 agent 替人查数"的能力。

## 8. 与老架构（legacy/v1）的对照

| | legacy/v1（Docker chat BI） | 当前主线（agent 引擎） |
|---|---|---|
| 形态 | docker-compose 多容器（wren-ui/ai-service/engine） | `pip install wrenai` 单进程 CLI + MCP |
| 入口 | Web 聊天 UI | Claude Code/Cursor 等 agent + CLI |
| 语义层 | MDL 存 UI 的 Postgres 里 | MDL = 项目内 YAML/文件，git 管理 |
| 引擎 | ibis-server 独立服务 | wren-core Rust 进程内调用 |
| 维护 | 冻结（仅 Classic 保留） | 活跃主线 |

## 9. 值得借鉴的设计点

1. **"指南住在 CLI 里"**：`wren skills get <name>` 按需下发与安装版本严格一致的工作流指南，agent 的 stub 只有 50 行——避免"文档写着 v1 代码已是 v2"的漂移，我们的 skills 体系可参考。
2. **开源边界切在"治理"与"UI"**：语义层/引擎/MCP 全开源，权限（RLS/CLS）和 UI 商业化——边界清晰、不损害自托管可用性。
3. **引擎 wasm 化**：同一个 Rust 引擎编译到 native + Python 绑定 + wasm 三端复用，dashboard 能纯浏览器端跑真 SQL 规划。
4. **敢冻结旧形态**：整个 Docker 产品线挪 legacy 分支、改名 Classic、明说不再修——比"背着历史包袱小修小补"干净得多。
5. **演进方向与趋势一致**：从"给人做的 chat BI"转向"给 agent 做的引擎"，本质是把 LLM 编排权还给 agent 宿主（Claude Code 等），自己只做最难做对的上下文与治理——这与我们 om-nlquery/beta 站的 wren 集成方向（wren 做语义层、上层 agent 提问）一致。

## 10. 快速索引

- 仓库：https://github.com/Canner/WrenAI （Rust 引擎并入后主仓）
- 旧引擎仓 `Canner/wren-engine`：已归档，代码在 `core/wren-core`
- Docker 版老产品：`legacy/v1` 分支，tag `v1-final`
- PyPI：`wrenai`（CLI/SDK）、`wren-core`（Python 绑定）、npm `wren-core-wasm`
- CLI 本机实测细节（context/profile/mcp/dbt/MCP tools 清单）：见同仓《wren-ai-链路与集成调研.md》
